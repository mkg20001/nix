=== INSTALLATION ===

Commands:

- nixos-generate-config --root /mnt
  Generate nixos config on /mnt

- DEVICE=device-name bootstrap
  Bootstrap config for device-name on $DEVICE

- nixos-install --root /mnt
  Install or rebuild os on /mnt
